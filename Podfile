# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'Plumedot' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Plumedot

  pod 'SwiftyJSON'
  pod 'SWXMLHash', '~> 7.0.0'
  pod 'Mantis', '~> 1.9.0'
  pod 'OpenAI'
  pod 'Firebase/Crashlytics'
  target 'PlumedotTests' do
    inherit! :search_paths
 
    # Pods for testing
  end

  target 'PlumedotUITests' do
    # Pods for testing
  end

end
